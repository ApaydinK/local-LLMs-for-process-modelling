process tree structure: +( 'send_invoice', +( *( *( 'receive_payment', *( 'verify_authentication', 'authenticate' ) ), X( 'process_order', *( 'check_inventory', 'verify_shipping' ) ) ), *( 'package_item', 'ship_package' ) ) )

The process starts with sending an invoice, which is the primary goal. Then, it involves receiving payment and verifying authentication. The payment must be received and authenticated to proceed.

Next, the process order includes checking inventory and verifying shipping details. These tasks are carried out simultaneously and independently.

Finally, the item is packaged and shipped to the customer.